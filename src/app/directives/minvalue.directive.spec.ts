import { MinvalueDirective } from './minvalue.directive';

describe('MinvalueDirective', () => {
  it('should create an instance', () => {
    const directive = new MinvalueDirective();
    expect(directive).toBeTruthy();
  });
});
